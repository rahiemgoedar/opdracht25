//Checking if a number is big
const biggerThenHundred = function (number) { //this functions does something: it checks.
   const checkNumber = number > 100; 
   return checkNumber;
}
const result = biggerThenHundred (101);
console.log(result); 

//Bouncer at a club
/* const brendaBouncer = function (nr1, nr2) {
const sum = nr1 && nr2; {
return "(nr1 => 400)" ;
}
return "come inside";
};
const result = brendaBouncer(399, 5);
console.log(result); */

//Calculate the average
